name: CI-CD Orchestrator

on:
 push:
   branches: [ main ]

jobs:
  sast:
    uses: KrakennTunisie/Devops-workflow/.github/workflows/SAST.yml@main

  buildAPP:
    needs: sast
    uses:  KrakennTunisie/Devops-workflow/.github/workflows/build.yml@main

  deloyment:
    needs: buildAPP
    uses:  KrakennTunisie/Devops-workflow/.github/workflows/deployment.yml@main
